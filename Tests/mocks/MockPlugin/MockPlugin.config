set(autostart ${PLUGIN_MOCKPLUGIN_AUTOSTART})

if(PLUGIN_MOCKPLUGIN_STARTUPORDER)
set (startuporder ${PLUGIN_MOCKPLUGIN_STARTUPORDER})
endif()

map()
   key(root)
   map()
       kv(mode ${PLUGIN_DUMMYPLUGIN_MODE})
       kv(locator lib${PLUGIN_IMPLEMENTATION}.so)
   end()
end()
ans(configuration)

